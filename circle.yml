general:
  artifacts:
    - "build/libs/wizard-0.0.1-SNAPSHOT.war" 
dependencies:
  pre:
    - gradle --version
  override:
    - gradle dependencies
test:
  override:
    - gradle build
  post:
    - mkdir -p $CIRCLE_TEST_REPORTS/junit/
    - find . -type f -regex ".*/build/test-results/.*xml" -exec cp {} $CIRCLE_TEST_REPORTS/junit/ \;