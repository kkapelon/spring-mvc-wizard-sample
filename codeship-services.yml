wizard-build:
  build:
    dockerfile_path: Dockerfile.build
  volumes:
      - /tmp/app:/tmp/app/
wizard:
  build:
    dockerfile_path: Dockerfile
  ports:
      - "8080:8080"
  volumes:
      - /tmp/app:/tmp/app
