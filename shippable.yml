language: java

jdk:
   - oraclejdk7

script:
   - gradle --version
   - lein --version
   - mvn --version
   - mvn test cobertura:cobertura

after_success:
   - mkdir shippable 
   - copy target/*.jar shippable 


archive: true