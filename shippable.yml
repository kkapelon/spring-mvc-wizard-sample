language: java

jdk:
   - oraclejdk7

script:
   - gradle --version
   - lein --version
   - mvn --version
   - mvn cobertura:cobertura package

after_success:
   - cp -r target/codecoverage shippable
   - cp-r target/testresults shippable
   - cp target/*.jar shippable 


archive: true